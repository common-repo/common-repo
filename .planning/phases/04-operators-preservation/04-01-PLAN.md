---
phase: 04-operators-preservation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/operators.rs, src/merge/yaml.rs, src/merge/json.rs, src/merge/toml.rs, src/merge/ini.rs, src/merge/markdown.rs, src/config.rs]
autonomous: true
requirements: [CODE-07]

must_haves:
  truths:
    - "The source: field in all merge operator structs (YamlMergeOp, JsonMergeOp, TomlMergeOp, IniMergeOp, MarkdownMergeOp) remains named source"
    - "get_source() methods on all merge op structs return the source field value unchanged"
    - "Merge operator apply functions read source: as a fragment file path, not a repository reference"
    - "No phase 1-3 rename accidentally changed the merge operator source: field to upstream or any other name"
  artifacts:
    - path: "src/config.rs"
      provides: "Merge operator structs with source field"
      contains: "source"
    - path: "src/merge/yaml.rs"
      provides: "YAML merge implementation using source field"
      contains: "get_source"
    - path: "src/merge/json.rs"
      provides: "JSON merge implementation using source field"
      contains: "get_source"
    - path: "src/merge/toml.rs"
      provides: "TOML merge implementation using source field"
      contains: "get_source"
    - path: "src/merge/ini.rs"
      provides: "INI merge implementation using source field"
      contains: "get_source"
    - path: "src/merge/markdown.rs"
      provides: "Markdown merge implementation using source field"
      contains: "get_source"
  key_links:
    - from: "src/config.rs"
      to: "src/merge/*.rs"
      via: "get_source() method on merge op structs"
      pattern: "get_source\\(\\)"
    - from: "src/operators.rs"
      to: "src/merge/*.rs"
      via: "dispatching merge operations"
      pattern: "apply_.*_merge"
---

<objective>
Audit and verify that the merge operator `source:` field was NOT accidentally renamed during phases 1-3 of the upstream terminology rename.

Purpose: The `source:` field in merge operators (yaml, json, toml, ini, markdown) refers to a fragment file path, NOT the repository. It must remain named "source" and must not have been caught up in the "source -> upstream" rename. This phase gates all subsequent work.

Output: Verified audit confirming all merge operator source: fields are intact, plus a regression test preventing future accidental renames.
</objective>

<execution_context>
@/Users/shakefu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shakefu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit merge operator source: field preservation across all formats</name>
  <files>src/config.rs, src/operators.rs, src/merge/yaml.rs, src/merge/json.rs, src/merge/toml.rs, src/merge/ini.rs, src/merge/markdown.rs</files>
  <action>
Systematically verify that the `source:` field has NOT been renamed in any merge operator code. Check each of these specific items:

1. In src/config.rs: Confirm each merge op struct (YamlMergeOp, JsonMergeOp, TomlMergeOp, IniMergeOp, and MarkdownMergeOp if it exists) still has a field named `source` (likely `pub source: Option<String>`). Confirm each struct's `get_source()` method still references `self.source` or `self.auto_merge`. Confirm builder methods named `source()` still exist.

2. In src/merge/yaml.rs: Confirm `apply_yaml_merge_operation` (or similar) calls `op.get_source()` to obtain the fragment file path. Confirm no reference to "upstream" where "source" (meaning fragment path) should be.

3. In src/merge/json.rs: Same check as yaml — `get_source()` used for fragment path.

4. In src/merge/toml.rs: Same check.

5. In src/merge/ini.rs: Same check.

6. In src/merge/markdown.rs: Same check.

7. In src/operators.rs: Confirm the operator dispatch code passes merge operations through without renaming the source field.

If ANY of these checks fail (i.e., "source" was renamed to "upstream" or something else in merge operator context), document exactly what was changed and where, so it can be reverted.

If all checks pass, document the verification results as confirmation that CODE-07 is satisfied.

Do NOT modify any files in this task — this is a read-only audit.
  </action>
  <verify>
    <automated>grep -c "get_source\(\)" src/merge/yaml.rs src/merge/json.rs src/merge/toml.rs src/merge/ini.rs src/merge/markdown.rs src/config.rs && grep -c "source:" src/config.rs | tail -1</automated>
  </verify>
  <done>Every merge operator struct retains its `source` field. Every merge implementation file calls `get_source()`. No accidental rename to "upstream" found in merge operator context. Audit results documented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression test guarding merge operator source: field</name>
  <files>tests/merge_source_field_guard.rs</files>
  <behavior>
    - Test 1: YamlMergeOp can be constructed with a `source` field and `get_source()` returns it
    - Test 2: JsonMergeOp can be constructed with a `source` field and `get_source()` returns it
    - Test 3: TomlMergeOp can be constructed with a `source` field and `get_source()` returns it
    - Test 4: IniMergeOp can be constructed with a `source` field and `get_source()` returns it
    - Test 5: MarkdownMergeOp (if it exists) can be constructed with a `source` field and `get_source()` returns it
  </behavior>
  <action>
Create a new test file `tests/merge_source_field_guard.rs` that serves as a regression guard. These tests will fail if anyone renames the `source` field on merge operator structs.

For each merge op struct (YamlMergeOp, JsonMergeOp, TomlMergeOp, IniMergeOp, and MarkdownMergeOp if it exists):
- Import the struct from `common_repo::config` (or wherever it is exported from the library crate)
- Use the builder pattern (e.g., `YamlMergeOp::default().source("fragment.yaml")`) to construct an instance with a source value
- Assert that `get_source()` returns `Some("fragment.yaml")`
- Add a doc comment on the test explaining WHY this test exists: the `source:` field refers to fragment file paths, not the repository, and must never be renamed to "upstream"

Check how the library crate exports these types (look at src/lib.rs for pub mod or pub use statements) and import accordingly. If the types are not publicly exported, add the necessary `pub` visibility or use `#[cfg(test)]` paths as appropriate for the project's test conventions.

Follow the project's existing test patterns — check tests/ directory for examples of how integration tests import from the crate.
  </action>
  <verify>
    <automated>cargo nextest run --test merge_source_field_guard</automated>
  </verify>
  <done>Regression test file exists at tests/merge_source_field_guard.rs. All tests pass. Tests would fail if source field were renamed to anything else on any merge operator struct.</done>
</task>

</tasks>

<verification>
1. Audit confirms all merge operator source: fields are intact (Task 1)
2. Regression tests pass, proving source field works correctly (Task 2)
3. `cargo nextest run --test merge_source_field_guard` exits 0
</verification>

<success_criteria>
- The `source:` field exists and functions correctly in all 5 merge operator structs
- No accidental rename from phases 1-3 detected
- Regression test prevents future accidental renames
- CODE-07 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-operators-preservation/04-01-SUMMARY.md`
</output>
