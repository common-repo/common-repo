---
phase: 01-config-structs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.rs
autonomous: true
requirements:
  - CONF-01
  - CONF-02

must_haves:
  truths:
    - "All doc comments on config struct fields that referenced 'source' (meaning the repository) now say 'upstream'"
    - "The merge operator source: field and its doc comments are unchanged (refers to fragment file path)"
    - "Code compiles after the rename with no new warnings"
  artifacts:
    - path: "src/config.rs"
      provides: "Config structs with updated doc comments"
      contains: "used as an upstream"
  key_links:
    - from: "src/config.rs"
      to: "downstream phases"
      via: "terminology foundation"
      pattern: "upstream"
---

<objective>
Update config struct doc comments in src/config.rs to replace "source" with "upstream" where the comments refer to the repository role (not the merge operator fragment path field).

Purpose: Establish the terminology foundation in config structs so all downstream phases build on consistent naming.
Output: Updated src/config.rs with "upstream" terminology in doc comments on config struct fields.
</objective>

<execution_context>
@/Users/shakefu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shakefu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update config struct doc comments from "source" to "upstream"</name>
  <files>src/config.rs</files>
  <action>
In src/config.rs, update the following doc comments that reference the repository role:

1. Lines with `/// Mark this operation as deferred (applies when repo is used as a source)` -- change to `/// Mark this operation as deferred (applies when repo is used as an upstream)`. These appear on the `defer` fields of:
   - YamlMergeOp (line ~183)
   - JsonMergeOp (line ~312)
   - TomlMergeOp (line ~340)
   - IniMergeOp (line ~575)
   - MarkdownMergeOp (line ~708)

2. The doc comment on Operation::is_deferred (line ~892): `/// Check if this operation is deferred (applies when repo is used as a source)` -- change to `/// Check if this operation is deferred (applies when repo is used as an upstream)`

3. Any doc comments with `/// Shorthand: sets source=dest to this value and implies defer=true` -- these refer to the merge operator source field behavior and should NOT be changed (the source field is a fragment path, not a repository).

IMPORTANT: Do NOT rename any of the following (they refer to merge operator fragment file paths, not the repository):
- The `pub source: Option<String>` fields on merge op structs
- The `pub fn source()` builder methods
- The `pub fn get_source()` methods
- Doc comments like "Source fragment file" that describe the fragment path field
- Error messages about "source and dest" in validation methods
- The `/// Set auto-merge mode (source=dest, implies defer)` comments (these describe the merge operator source field behavior)

This is a hard rename with no backwards compatibility shims needed (CONF-02).
  </action>
  <verify>
    <automated>cd /Users/shakefu/git/common-repo/common-repo && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 doc comments referencing "repo is used as a source" now say "repo is used as an upstream". Merge operator source: field and its documentation are untouched. Code compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Verify no remaining "source repo" references in config struct documentation</name>
  <files>src/config.rs</files>
  <action>
Search src/config.rs for any remaining doc comments that use "source" to mean the repository role (not the merge operator fragment path). Specifically:

1. Run: grep -n "as a source" src/config.rs -- should return zero results
2. Run: grep -n "source repo" src/config.rs -- should return zero results
3. Run: grep -n "/// .*source" src/config.rs -- review each match to confirm it refers to the merge operator fragment path, not the repository

If any straggling "source" references that mean "repository" are found, update them to "upstream".

Then run the full test suite to confirm nothing broke: cargo nextest run (or cargo test as fallback).
  </action>
  <verify>
    <automated>cd /Users/shakefu/git/common-repo/common-repo && grep -c "as a source" src/config.rs | grep -q "^0$" && echo "PASS: no 'as a source' remaining" && cargo check 2>&1 | tail -3</automated>
  </verify>
  <done>Zero doc comments in src/config.rs reference "source" meaning the repository role. All remaining "source" references are about the merge operator fragment file path. Code compiles and tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -n "as a source" src/config.rs` returns no matches
2. `grep -n "as an upstream" src/config.rs` returns 6 matches (one per merge op struct + one on Operation::is_deferred)
3. `cargo check` succeeds with no errors or warnings
4. Merge operator `source:` fields, builder methods, and their doc comments are unchanged
</verification>

<success_criteria>
- All config struct doc comments that referenced the repository as "source" now say "upstream"
- Merge operator source: field documentation is preserved unchanged
- `cargo check` passes
- No backwards compatibility shims exist (hard rename per CONF-02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-config-structs/01-01-SUMMARY.md`
</output>
