---
phase: 07-test-updates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/cli_e2e_update.rs
  - tests/cli_e2e_defer.rs
autonomous: true
requirements:
  - TEST-02

must_haves:
  truths:
    - "cli_e2e_update.rs assertions match the CLI output strings changed in Phase 6"
    - "cli_e2e_defer.rs doc comment uses upstream terminology"
    - "No test logic or coverage has been removed or weakened"
  artifacts:
    - path: "tests/cli_e2e_update.rs"
      provides: "E2E tests for update command with correct upstream output assertions"
      contains: "Filtering upstreams matching"
    - path: "tests/cli_e2e_defer.rs"
      provides: "E2E tests for defer command with correct doc comment"
      contains: "upstream repositories"
  key_links:
    - from: "tests/cli_e2e_update.rs"
      to: "src/commands/update.rs"
      via: "CLI output string assertions"
      pattern: "Filtering upstreams"
---

<objective>
Update test assertions in cli_e2e_update.rs and a doc comment in cli_e2e_defer.rs to match the "source" to "upstream" terminology changes made in Phase 6.

Purpose: These test files assert on CLI output strings that Phase 6 changed ("Filtering sources" became "Filtering upstreams"). Without updating, these tests will fail.
Output: Two test files with corrected assertions and doc comment. Identical test coverage.
</objective>

<execution_context>
@/Users/shakefu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shakefu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-test-updates/07-RESEARCH.md
@tests/cli_e2e_update.rs
@tests/cli_e2e_defer.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLI output assertions in cli_e2e_update.rs and doc comment in cli_e2e_defer.rs</name>
  <files>tests/cli_e2e_update.rs, tests/cli_e2e_defer.rs</files>
  <action>
In `tests/cli_e2e_update.rs`, update these 5 string assertions that check CLI output changed by Phase 6:

| Approx Line | Old String | New String |
|-------------|-----------|------------|
| 533 | `"Filter sources by glob pattern"` | `"Filter upstreams by glob pattern"` |
| 567 | `"Filtering sources matching"` | `"Filtering upstreams matching"` |
| 598 | `"Filtering sources matching"` | `"Filtering upstreams matching"` |
| 635 | `"Filtering sources matching: gitlab.com/*, github.com/common-repo/*"` | `"Filtering upstreams matching: gitlab.com/*, github.com/common-repo/*"` |
| 669 | `"Filtering sources matching"` | `"Filtering upstreams matching"` |

Use exact search-and-replace. Do NOT change any other "source" references in this file -- many refer to the merge operator `source:` field or file paths which must stay as-is.

In `tests/cli_e2e_defer.rs`, update line 4 doc comment only:
- "source repositories" -> "upstream repositories"

Do NOT change any other content in cli_e2e_defer.rs. The `source:` fields in YAML test data refer to merge operator fragment paths and must remain unchanged.
  </action>
  <verify>
    <automated>cd /Users/shakefu/git/common-repo/common-repo && grep -c "Filtering sources\|Filter sources" tests/cli_e2e_update.rs | grep -q "^0$" && grep -c "Filtering upstreams\|Filter upstreams" tests/cli_e2e_update.rs | grep -q "^5$" && grep "upstream repositories" tests/cli_e2e_defer.rs | head -1 && echo "PASS: assertions updated"</automated>
  </verify>
  <done>
- All 5 "sources" assertions in cli_e2e_update.rs now say "upstreams"
- cli_e2e_defer.rs doc comment says "upstream repositories"
- No other content in either file has been changed
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify test compilation for updated files</name>
  <files>tests/cli_e2e_update.rs, tests/cli_e2e_defer.rs</files>
  <action>
Run `cargo test --test cli_e2e_update --no-run` and `cargo test --test cli_e2e_defer --no-run` to verify both files compile cleanly after the string changes.

Also verify no unintended "source" references were changed by running `grep -n "source" tests/cli_e2e_update.rs` and confirming all remaining "source" references are merge operator `source:` fields, file path variables, or other non-repository uses.
  </action>
  <verify>
    <automated>cd /Users/shakefu/git/common-repo/common-repo && cargo test --test cli_e2e_update --no-run 2>&1 | tail -3 && cargo test --test cli_e2e_defer --no-run 2>&1 | tail -3</automated>
  </verify>
  <done>
- cargo test --test cli_e2e_update --no-run succeeds
- cargo test --test cli_e2e_defer --no-run succeeds
- All remaining "source" references in cli_e2e_update.rs are non-repository uses (merge operator fields, file paths)
  </done>
</task>

</tasks>

<verification>
1. `grep -c "Filtering sources\|Filter sources" tests/cli_e2e_update.rs` -- outputs 0
2. `grep -c "Filtering upstreams\|Filter upstreams" tests/cli_e2e_update.rs` -- outputs 5
3. `grep "upstream repositories" tests/cli_e2e_defer.rs` -- matches doc comment
4. `cargo test --test cli_e2e_update --no-run` -- compiles
5. `cargo test --test cli_e2e_defer --no-run` -- compiles
</verification>

<success_criteria>
- All 5 CLI output assertions in cli_e2e_update.rs updated from "sources" to "upstreams"
- Doc comment in cli_e2e_defer.rs updated from "source repositories" to "upstream repositories"
- Both files compile cleanly
- No merge operator source: references or file path variables were changed
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-updates/07-02-SUMMARY.md`
</output>
