---
phase: 07-test-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/cli_e2e_upstream_ops.rs
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "Test file cli_e2e_source_ops.rs no longer exists"
    - "Test file cli_e2e_upstream_ops.rs exists with identical test coverage"
    - "All test function names, doc comments, variable names, and string literals use upstream instead of source where referring to the upstream repository"
    - "No tests have been removed or weakened"
  artifacts:
    - path: "tests/cli_e2e_upstream_ops.rs"
      provides: "E2E tests for upstream repository operation semantics"
      min_lines: 1070
  key_links:
    - from: "tests/cli_e2e_upstream_ops.rs"
      to: "cargo_bin_cmd!(\"common-repo\")"
      via: "assert_cmd CLI invocation"
      pattern: "cargo_bin_cmd!"
---

<objective>
Rename the test file cli_e2e_source_ops.rs to cli_e2e_upstream_ops.rs and update all internal terminology from "source" to "upstream" where it refers to the upstream repository concept.

Purpose: Align test files with the upstream terminology rename completed in phases 1-6.
Output: Renamed test file with updated identifiers, comments, and string literals. Identical test coverage.
</objective>

<execution_context>
@/Users/shakefu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shakefu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/cli_e2e_source_ops.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename test file and update all source-to-upstream terminology</name>
  <files>tests/cli_e2e_source_ops.rs, tests/cli_e2e_upstream_ops.rs</files>
  <action>
Use `git mv tests/cli_e2e_source_ops.rs tests/cli_e2e_upstream_ops.rs` to rename the file while preserving git history.

Then update the contents of the renamed file. Apply these replacements throughout:

**Doc comments (module-level and per-function):**
- "source repository" -> "upstream repository" (and "source repo" -> "upstream repo")
- "source repositories" -> "upstream repositories"
- "Source repo" -> "Upstream repo" (capitalized)
- "Source's" -> "Upstream's"
- "source's" -> "upstream's"
- "in source:" -> "in upstream:" (in semantic descriptions)
- "Source ops" -> "Upstream ops"
- "source ops" -> "upstream ops"
- "source-declared" -> "upstream-declared"

**Variable names:**
- `source_repo` -> `upstream_repo` (the TempDir variable, used ~12 times)
- `source_url` -> `upstream_url` (the format! URL variable, used ~12 times)

**Test function names:**
- `test_source_config_file_not_copied` -> `test_upstream_config_file_not_copied`
- `test_source_include_operator_respected` -> `test_upstream_include_operator_respected`
- `test_source_exclude_operator_respected` -> `test_upstream_exclude_operator_respected`
- `test_operation_order_source_then_consumer` -> `test_operation_order_upstream_then_consumer`
- `test_source_rename_operator_respected` -> `test_upstream_rename_operator_respected`
- `test_source_combined_operations` -> `test_upstream_combined_operations`
- `test_alternate_config_filename_not_copied` -> keep as-is (no "source" in name)
- `test_real_world_shakefu_vibes_repo` -> keep as-is (no "source" in name)
- `test_ls_respects_source_declared_excludes` -> `test_ls_respects_upstream_declared_excludes`
- `test_consumer_with_template_vars_override_source_defaults` -> `test_consumer_with_template_vars_override_upstream_defaults`
- `test_consumer_top_level_template_vars_override_source_defaults` -> `test_consumer_top_level_template_vars_override_upstream_defaults`

**Section comment headers (the `// ====` blocks):**
- "Source's .common-repo.yaml" -> "Upstream's .common-repo.yaml"
- "Source's include operator" -> "Upstream's include operator"
- "Source's exclude operator" -> "Upstream's exclude operator"
- "Operation order: Source ops then consumer" -> "Operation order: Upstream ops then consumer"
- "Source's rename operator" -> "Upstream's rename operator"
- "Source-declared template vars" -> "Upstream-declared template vars"

**String literals in test fixtures (inline YAML/assertions):**
- `"# Source repo config"` -> `"# Upstream repo config"`
- `"Source repo config"` -> `"Upstream repo config"` (in assert! checks)
- `"Source's .common-repo.yaml"` -> `"Upstream's .common-repo.yaml"` (in assertion messages)
- `"# Source README"` -> `"# Upstream README"`
- `"// Source library"` -> `"// Upstream library"`
- `"# Source declares its public API"` -> `"# Upstream declares its public API"`
- `"# Source excludes internal files"` -> `"# Upstream excludes internal files"`
- `"# Source only exposes public/ directory"` -> `"# Upstream only exposes public/ directory"`
- `"# Source renames template files"` -> `"# Upstream renames template files"`
- `"source-excluded"` -> `"upstream-excluded"` (in assertion messages)
- `"Source's non-overridden"` -> `"Upstream's non-overridden"` (in assertion messages)
- `"Consumer's .common-repo.yaml should NOT be overwritten by source's config"` -> replace "source's" with "upstream's"

**DO NOT rename:**
- The `source:` field inside merge operator YAML (not present in this file, but be aware)
- The `init_git_repo` helper function name (not source-related)
- The word "source" in `"// Source library"` style inline file content -- actually DO rename these since they refer to the upstream repo's files
- Any import statements or external crate references

**Preserve exactly:**
- All test logic, assertions, and control flow
- All file paths in test fixtures
- The `init_git_repo` helper function (unchanged)
- All `#[test]` and `#[cfg_attr(...)]` attributes
- The `use` imports block
  </action>
  <verify>
    <automated>cd /Users/shakefu/git/common-repo/common-repo && test ! -f tests/cli_e2e_source_ops.rs && test -f tests/cli_e2e_upstream_ops.rs && grep -c "source_repo\|source_url\|test_source_" tests/cli_e2e_upstream_ops.rs | grep -q "^0$" && echo "PASS: file renamed and terminology updated"</automated>
  </verify>
  <done>
- tests/cli_e2e_source_ops.rs no longer exists
- tests/cli_e2e_upstream_ops.rs exists with all test functions renamed
- No variable named source_repo or source_url remains
- No test function name contains "source_" (as prefix referring to upstream concept)
- File has same number of #[test] attributes (12 tests)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify test compilation and count</name>
  <files>tests/cli_e2e_upstream_ops.rs</files>
  <action>
Run `cargo test --test cli_e2e_upstream_ops --no-run` to verify the renamed test file compiles without errors.

Then run `grep -c '#\[test\]' tests/cli_e2e_upstream_ops.rs` to confirm the test count is exactly 12 (same as original).

Also run `grep -c 'source' tests/cli_e2e_upstream_ops.rs` and manually audit any remaining "source" references to confirm they are NOT referring to the upstream repository concept. Acceptable remaining uses of "source":
- None expected in this file after the rename

If any "source" references remain that should be "upstream", fix them.
  </action>
  <verify>
    <automated>cd /Users/shakefu/git/common-repo/common-repo && cargo test --test cli_e2e_upstream_ops --no-run 2>&1 | tail -5</automated>
  </verify>
  <done>
- cargo test --test cli_e2e_upstream_ops --no-run succeeds (compiles cleanly)
- Test count is exactly 12
- No remaining "source" references that should be "upstream"
  </done>
</task>

</tasks>

<verification>
1. `test ! -f tests/cli_e2e_source_ops.rs` -- old file removed
2. `test -f tests/cli_e2e_upstream_ops.rs` -- new file exists
3. `cargo test --test cli_e2e_upstream_ops --no-run` -- compiles
4. `grep -c '#\[test\]' tests/cli_e2e_upstream_ops.rs` -- outputs 12
5. `grep -c 'source_repo\|source_url' tests/cli_e2e_upstream_ops.rs` -- outputs 0
</verification>

<success_criteria>
- Test file renamed from cli_e2e_source_ops.rs to cli_e2e_upstream_ops.rs
- All 12 tests preserved with updated function names
- All variable names, doc comments, and string literals use "upstream" instead of "source" (where referring to the upstream repository)
- File compiles cleanly with cargo test --no-run
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-updates/07-01-SUMMARY.md`
</output>
