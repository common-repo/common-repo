---
phase: 02-core-code-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phases/mod.rs
  - src/phases/processing.rs
  - src/phases/composite.rs
autonomous: true
requirements:
  - CODE-01
must_haves:
  truths:
    - "No struct field or variable named source_url or source_ref exists in src/phases/ where it refers to the upstream repository"
    - "IntermediateFS fields are named upstream_url and upstream_ref"
    - "Code compiles cleanly after all identifier renames"
  artifacts:
    - path: "src/phases/mod.rs"
      provides: "IntermediateFS struct with upstream_url and upstream_ref fields"
      contains: "pub upstream_url"
    - path: "src/phases/processing.rs"
      provides: "Updated call sites and test assertions using upstream_url/upstream_ref"
      contains: "upstream_url"
    - path: "src/phases/composite.rs"
      provides: "Updated IntermediateFS constructor calls with upstream_url/upstream_ref params"
      contains: "upstream_url"
  key_links:
    - from: "src/phases/processing.rs"
      to: "src/phases/mod.rs"
      via: "IntermediateFS::new_with_vars_and_merges constructor"
      pattern: "IntermediateFS::new_with_vars_and_merges"
    - from: "src/phases/composite.rs"
      to: "src/phases/mod.rs"
      via: "IntermediateFS::new and new_with_vars constructors in tests"
      pattern: "IntermediateFS::new"
---

<objective>
Rename IntermediateFS struct fields `source_url` and `source_ref` to `upstream_url` and `upstream_ref`, and update all call sites across the phases module.

Purpose: These fields track which repository an intermediate filesystem came from. They refer to the upstream repository, not a generic "source", so they should use "upstream" terminology (CODE-01).
Output: All three files updated, code compiles cleanly.
</objective>

<execution_context>
@/Users/shakefu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shakefu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/phases/mod.rs
@src/phases/processing.rs
@src/phases/composite.rs

<interfaces>
<!-- IntermediateFS struct definition in src/phases/mod.rs (lines 143-197) -->
<!-- Fields to rename: source_url -> upstream_url, source_ref -> upstream_ref -->
<!-- Three constructors: new(), new_with_vars(), new_with_vars_and_merges() -->
<!-- All take source_url: String and source_ref: String params that must be renamed -->

From src/phases/mod.rs:
```rust
pub struct IntermediateFS {
    pub fs: MemoryFS,
    pub source_url: String,    // RENAME -> upstream_url
    pub source_ref: String,    // RENAME -> upstream_ref
    pub template_vars: HashMap<String, String>,
    pub merge_operations: Vec<Operation>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename IntermediateFS fields and constructor params in mod.rs</name>
  <files>src/phases/mod.rs</files>
  <action>
In src/phases/mod.rs, rename the IntermediateFS struct fields and all constructor parameters:

1. Rename struct field `source_url: String` to `upstream_url: String` (line 147)
2. Rename struct field `source_ref: String` to `upstream_ref: String` (line 149)
3. Update doc comment on source_url field: "Repository URL this FS came from" is fine, no change needed
4. In `fn new()`: rename params `source_url` -> `upstream_url`, `source_ref` -> `upstream_ref`, and update field assignments
5. In `fn new_with_vars()`: same param renames and field assignments
6. In `fn new_with_vars_and_merges()`: same param renames and field assignments

Do NOT rename any other identifiers in this file. Only the IntermediateFS source_url/source_ref fields and their constructor params.
  </action>
  <verify>
    <automated>cd /Users/shakefu/git/common-repo/common-repo && grep -n "source_url\|source_ref" src/phases/mod.rs | grep -v "^#" ; test $? -eq 1</automated>
  </verify>
  <done>IntermediateFS struct and all three constructors use upstream_url and upstream_ref. No source_url or source_ref remains in mod.rs.</done>
</task>

<task type="auto">
  <name>Task 2: Update all IntermediateFS call sites in processing.rs and composite.rs</name>
  <files>src/phases/processing.rs, src/phases/composite.rs</files>
  <action>
Update all code that references IntermediateFS.source_url or IntermediateFS.source_ref:

**src/phases/processing.rs:**
1. Line 1760: Change `assert_eq!(intermediate.source_url, "local")` to `assert_eq!(intermediate.upstream_url, "local")`
2. Line 1761: Change `assert_eq!(intermediate.source_ref, "HEAD")` to `assert_eq!(intermediate.upstream_ref, "HEAD")`
3. No changes needed to production code in processing.rs -- the constructor calls pass positional args, so only the callee param names matter (already renamed in Task 1)

**src/phases/composite.rs:**
1. No changes needed -- all IntermediateFS usages in composite.rs are constructor calls with positional arguments (not named fields). The `source_fs` parameter in `merge_filesystem()` refers to the input filesystem being merged, NOT to "source repo", so it must NOT be renamed.
2. The `source_vars` variable in test code (lines 848-857) is a local test variable for template variables, NOT a "source repo" reference -- do NOT rename.

After edits, run `cargo check` to verify compilation.
  </action>
  <verify>
    <automated>cd /Users/shakefu/git/common-repo/common-repo && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>All references to IntermediateFS.source_url and .source_ref across the codebase now use upstream_url and upstream_ref. `cargo check` succeeds with no errors.</done>
</task>

</tasks>

<verification>
1. `grep -rn "source_url\|source_ref" src/phases/mod.rs` returns no matches
2. `grep -rn "\.source_url\|\.source_ref" src/phases/` returns no matches (field access)
3. `cargo check` passes with no errors
4. `cargo nextest run --filter-expr 'test(intermediate)' 2>&1 | tail -10` -- any IntermediateFS-related tests pass
</verification>

<success_criteria>
- IntermediateFS struct fields are upstream_url and upstream_ref
- All constructor parameters renamed to match
- All field access sites updated (processing.rs test assertions)
- cargo check passes cleanly
- No accidental renames of unrelated source_ identifiers (source_fs in merge_filesystem, source_vars in tests)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-code-rename/02-01-SUMMARY.md`
</output>
