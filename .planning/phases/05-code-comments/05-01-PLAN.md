---
phase: 05-code-comments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phases/discovery.rs
  - src/phases/processing.rs
  - src/phases/composite.rs
autonomous: true
requirements:
  - CODE-02

must_haves:
  truths:
    - "No code comment in src/phases/ refers to 'source repo' or 'source repository' where it means the upstream repository"
    - "Comments about merge operator source: field still correctly say 'source'"
    - "Comments are accurate and consistent with renamed identifiers from prior phases"
  artifacts:
    - path: "src/phases/discovery.rs"
      provides: "Updated doc comments and inline comments using upstream terminology"
      contains: "upstream repo"
    - path: "src/phases/processing.rs"
      provides: "Updated doc comments and inline comments using upstream terminology"
      contains: "upstream repo"
    - path: "src/phases/composite.rs"
      provides: "Updated test comments using upstream terminology"
      contains: "upstream repo"
  key_links: []
---

<objective>
Update all code comments in src/phases/ files to use "upstream repo" instead of "source repo".

Purpose: Phase 5 of the upstream terminology rename — comments in the phases directory reference "source repo" when they mean the upstream repository providing files to consumers.
Output: Three files with updated comments, no functional code changes.
</objective>

<execution_context>
@/Users/shakefu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shakefu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update comments in src/phases/discovery.rs</name>
  <files>src/phases/discovery.rs</files>
  <action>
Update the following comment lines to replace "source repo" / "source repository" / "source repos" with "upstream repo" / "upstream repository" / "upstream repos":

- Line 16: module doc comment "When fetching a source repository's config" -> "When fetching an upstream repository's config"
- Line 19: module doc comment "source repos to declare" -> "upstream repos to declare"
- Line 113: inline comment "Extract deferred operations from the source repo's config" -> "Extract deferred operations from the upstream repo's config"
- Line 203: doc comment "Extract deferred operations from a source repository's config" -> "Extract deferred operations from an upstream repository's config"
- Line 215: doc comment "Extract source operations from a source repository's config" -> "Extract upstream operations from an upstream repository's config"
- Line 218: doc comment "how a source repo exposes files" -> "how an upstream repo exposes files"

Do NOT rename any Rust identifiers, function names, or variable names — only comments and doc comments.
  </action>
  <verify>
    <automated>grep -n "source repo\|source repository" src/phases/discovery.rs | grep -c "." | xargs test 0 -eq</automated>
  </verify>
  <done>Zero occurrences of "source repo" or "source repository" in discovery.rs comments. All now say "upstream repo" or "upstream repository".</done>
</task>

<task type="auto">
  <name>Task 2: Update comments in src/phases/processing.rs and src/phases/composite.rs</name>
  <files>src/phases/processing.rs, src/phases/composite.rs</files>
  <action>
In src/phases/processing.rs, update these comment lines:

- Line 113: "Remove source repo's config files" -> "Remove upstream repo's config files"
- Line 208: "Remove source repository's config files" -> "Remove upstream repository's config files"
- Line 211: "copied from source repos to consumers" -> "copied from upstream repos to consumers"
- Line 212: "fetching a source repository but before" -> "fetching an upstream repository but before"

In src/phases/composite.rs, update these comment lines:

- Line 832: "Simulates: source repo declares template" -> "Simulates: upstream repo declares template"
- Line 841: "Mark as template (source repo declared template:)" -> "Mark as template (upstream repo declared template:)"

Do NOT rename any Rust identifiers — only comments and doc comments.
  </action>
  <verify>
    <automated>grep -n "source repo\|source repository" src/phases/processing.rs src/phases/composite.rs | grep -c "." | xargs test 0 -eq</automated>
  </verify>
  <done>Zero occurrences of "source repo" or "source repository" in processing.rs and composite.rs comments.</done>
</task>

</tasks>

<verification>
Run: `grep -rn "source repo\|source repository" src/phases/` — must return zero results.
Run: `cargo check` — code still compiles (comments only, so this is a sanity check).
</verification>

<success_criteria>
All comments in src/phases/ that previously said "source repo" now say "upstream repo". No functional code changes. Code compiles.
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-comments/05-01-SUMMARY.md`
</output>
