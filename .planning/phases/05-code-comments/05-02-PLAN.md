---
phase: 05-code-comments
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.rs
autonomous: true
requirements:
  - CODE-02

must_haves:
  truths:
    - "No code comment in src/config.rs refers to 'used as a source' where it means the upstream repository role"
    - "Comments about the merge operator source: field, source path, source=dest remain unchanged"
    - "Comments are accurate and consistent with renamed identifiers from prior phases"
  artifacts:
    - path: "src/config.rs"
      provides: "Updated doc comments using upstream terminology for repo role"
      contains: "used as an upstream"
  key_links: []
---

<objective>
Update code comments in src/config.rs that reference the "source" repository role to use "upstream" terminology. Carefully preserve all comments about the merge operator `source:` field.

Purpose: Phase 5 of the upstream terminology rename — config.rs has comments saying "used as a source" meaning the repo role, which must change to "upstream". Many other comments reference the `source:` field of merge operators, which must NOT change.
Output: src/config.rs with updated repo-role comments, merge operator comments untouched.
</objective>

<execution_context>
@/Users/shakefu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shakefu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update repo-role comments in src/config.rs</name>
  <files>src/config.rs</files>
  <action>
Update ONLY the comments that refer to the repository role "used as a source". Change "used as a source" to "used as an upstream" in these doc comments:

- Line 183: "Mark this operation as deferred (applies when repo is used as a source)" -> "Mark this operation as deferred (applies when repo is used as an upstream)"
- Line 312: same pattern
- Line 340: same pattern
- Line 575: same pattern
- Line 708: same pattern
- Line 892: "Check if this operation is deferred (applies when repo is used as a source)" -> "Check if this operation is deferred (applies when repo is used as an upstream)"

Also check the associated "Shorthand:" comments on lines 186, 315, 343, 578, 711 — if they say "source" in a repo-role context, update those too.

DO NOT CHANGE any of these (they refer to the merge operator source: field or source file path):
- Comments containing "source path", "source field", "source=dest", "source file"
- Comments containing ".source(" (builder method for setting source file path)
- Comments containing "Get the effective source path"
- Comments containing "Set the source file path"
- Comments containing "auto_merge or source"
- Comments containing "source/dest" (referring to the source and dest fields of operators)
- Line 2298: "source/dest are now Optional" (refers to struct fields, not repo role)

This is a comments-only change. Do NOT rename any Rust identifiers, function names, struct fields, or variable names.
  </action>
  <verify>
    <automated>grep -n "used as a source" src/config.rs | grep -c "." | xargs test 0 -eq</automated>
  </verify>
  <done>All "used as a source" comments in config.rs now say "used as an upstream". All merge operator source: field comments remain unchanged. No functional code changes.</done>
</task>

</tasks>

<verification>
Run: `grep -n "used as a source" src/config.rs` — must return zero results.
Run: `grep -c "source" src/config.rs` — should still show many results (the merge operator field references are preserved).
Run: `cargo check` — code still compiles.
</verification>

<success_criteria>
All repo-role "source" comments in src/config.rs now say "upstream". Merge operator source: field comments are untouched. Code compiles.
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-comments/05-02-SUMMARY.md`
</output>
