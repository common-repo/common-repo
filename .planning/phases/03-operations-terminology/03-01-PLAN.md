---
phase: 03-operations-terminology
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phases/discovery.rs
  - src/phases/processing.rs
autonomous: true
requirements:
  - CODE-03
  - CODE-04
  - CODE-05
  - CODE-06

must_haves:
  truths:
    - "extract_source_operations is renamed to extract_upstream_operations everywhere"
    - "source_filtering_ops variable is renamed to upstream_filtering_ops"
    - "Comments say upstream-declared instead of source-declared"
    - "Comments say upstream filtering instead of source filtering"
    - "Test module extract_source_ops_tests is renamed to extract_upstream_ops_tests"
    - "No source authors references exist in code (verified absent)"
    - "Code compiles after all renames"
  artifacts:
    - path: "src/phases/discovery.rs"
      provides: "Renamed function, variable, test module, and comments"
      contains: "fn extract_upstream_operations"
    - path: "src/phases/processing.rs"
      provides: "Updated comment referencing upstream operations"
      contains: "upstream operations"
  key_links:
    - from: "src/phases/discovery.rs"
      to: "extract_upstream_operations"
      via: "function call on line 111"
      pattern: "extract_upstream_operations"
    - from: "src/phases/discovery.rs"
      to: "upstream_filtering_ops"
      via: "variable binding on line 111"
      pattern: "let upstream_filtering_ops"
---

<objective>
Rename all operations-related terminology from "source" to "upstream" in the phase orchestration code.

Purpose: This phase targets the operations-level terminology (source-declared, source filtering, source authors, source_ops) that describes how upstream repos expose files to consumers. After phases 1-2 renamed config structs and core identifiers, this phase completes the operations vocabulary rename.

Output: discovery.rs and processing.rs with all operations terminology using "upstream" instead of "source"
</objective>

<execution_context>
@/Users/shakefu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shakefu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key identifiers being renamed in this plan -->

From src/phases/discovery.rs (current names -> new names):
- fn extract_source_operations() -> fn extract_upstream_operations()
- let source_filtering_ops = -> let upstream_filtering_ops =
- mod extract_source_ops_tests -> mod extract_upstream_ops_tests
- Comment "Source filtering ops" -> "Upstream filtering ops"
- Comment "source-declared merge behavior" -> "upstream-declared merge behavior"
- Comment "Extract source operations" -> "Extract upstream operations"
- Comment "Source operations are include..." -> "Upstream operations are include..."
- Comment "source repo exposes files" -> "upstream repo exposes files"
- Test fn name test_extracts_all_source_operations_including_templates -> test_extracts_all_upstream_operations_including_templates
- Comment "Non-source operations" -> "Non-upstream operations"

From src/phases/processing.rs:
- Comment "Source operations come first (from extract_source_operations)" -> "Upstream operations come first (from extract_upstream_operations)"

Note: The variable `source_vars` in processing.rs and composite.rs refers to template variable values (HashMap contents), NOT to source/upstream repository terminology. These are test helper variables holding string values like "GH_APP_OWNER" and should NOT be renamed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename operations terminology in discovery.rs</name>
  <files>src/phases/discovery.rs</files>
  <action>
Rename the following in src/phases/discovery.rs:

1. Function name: `extract_source_operations` -> `extract_upstream_operations` (line 220, definition)
2. All call sites of extract_source_operations -> extract_upstream_operations (lines 111, 1349, 1376, 1391, 1422, 1445, 1471, 1491, 1512, 1544, 1601)
3. Variable name: `source_filtering_ops` -> `upstream_filtering_ops` (lines 111, 128)
4. Test module name: `extract_source_ops_tests` -> `extract_upstream_ops_tests` (line 1328)
5. Test function name: `test_extracts_all_source_operations_including_templates` -> `test_extracts_all_upstream_operations_including_templates` (line 1562)

6. Update comments:
   - Line 109: "Extract source operations" -> "Extract upstream operations"
   - Line 110: "the source exposes" -> "the upstream repo exposes"
   - Line 113: "from the source repo's config" -> "from the upstream repo's config"
   - Line 125: "Source filtering ops" -> "Upstream filtering ops"
   - Line 126: "source-declared merge behavior" -> "upstream-declared merge behavior"
   - Line 215: "Extract source operations from a source repository's config" -> "Extract upstream operations from an upstream repository's config"
   - Line 217: "Source operations are include, exclude..." -> "Upstream operations are include, exclude..."
   - Line 218: "how a source repo exposes" -> "how an upstream repo exposes"
   - Line 1325: "Tests for extract_source_operations" -> "Tests for extract_upstream_operations"
   - Line 1591: "Non-source operations" -> "Non-upstream operations"

DO NOT rename:
- `source:` field references in merge operators (out of scope, preserved per CODE-07)
- `source_vars` in test helper code (refers to template variable values, not repository terminology)
  </action>
  <verify>
    <automated>cd /Users/shakefu/git/common-repo/common-repo && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>All occurrences of extract_source_operations, source_filtering_ops, extract_source_ops_tests, and related comments in discovery.rs use "upstream" terminology. Code compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Rename operations terminology in processing.rs and verify completeness</name>
  <files>src/phases/processing.rs</files>
  <action>
1. Update comment in processing.rs line 1294:
   "Source operations come first (from extract_source_operations)" -> "Upstream operations come first (from extract_upstream_operations)"

2. Verify CODE-05 (source authors): Confirm no "source author" references exist in src/ directory. This term does not appear in the codebase and requires no changes. Note this in the summary as verified-absent.

3. Run a grep to confirm no remaining "source_ops", "source_filtering", "source-declared", or "extract_source_operations" references exist in src/phases/discovery.rs or src/phases/processing.rs (excluding merge operator `source:` field references and `source_vars` test helpers).
  </action>
  <verify>
    <automated>cd /Users/shakefu/git/common-repo/common-repo && cargo check 2>&1 | tail -5 && echo "---" && grep -n "extract_source_operations\|source_filtering_ops\|source-declared\|extract_source_ops_tests" src/phases/discovery.rs src/phases/processing.rs; echo "Exit: $?"</automated>
  </verify>
  <done>processing.rs comment updated. No remaining "source" operations terminology in discovery.rs or processing.rs (except preserved source: merge operator field and source_vars test helpers). cargo check passes. CODE-05 verified absent from codebase.</done>
</task>

</tasks>

<verification>
1. `cargo check` passes with no errors
2. `grep -rn "extract_source_operations\|source_filtering_ops\|source-declared\|extract_source_ops_tests" src/phases/discovery.rs src/phases/processing.rs` returns no matches
3. `grep -n "extract_upstream_operations" src/phases/discovery.rs` returns the function definition and all call sites
4. `grep -n "upstream_filtering_ops" src/phases/discovery.rs` returns the variable binding and usage
</verification>

<success_criteria>
- extract_source_operations renamed to extract_upstream_operations (function + all call sites)
- source_filtering_ops renamed to upstream_filtering_ops (variable + usage)
- extract_source_ops_tests renamed to extract_upstream_ops_tests (test module)
- All related comments updated to use "upstream" instead of "source"
- processing.rs comment updated
- CODE-05 (source authors) verified absent -- no code changes needed
- cargo check passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-operations-terminology/03-01-SUMMARY.md`
</output>
