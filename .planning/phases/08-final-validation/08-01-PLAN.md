---
phase: 08-final-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["(any files with test/CI failures)"]
autonomous: true
requirements: [TEST-03, TEST-04]

must_haves:
  truths:
    - "./script/test exits 0 with zero test failures"
    - "./script/ci exits 0 (fmt, clippy, pre-commit, prose all green)"
  artifacts:
    - path: "(no new files — this plan fixes existing files)"
      provides: "Passing test suite and CI checks"
  key_links:
    - from: "./script/test"
      to: "all test files in tests/ and src/"
      via: "cargo nextest run"
      pattern: "test result: ok"
    - from: "./script/ci"
      to: "cargo fmt, clippy, prek, prose checks"
      via: "./script/ci pipeline"
      pattern: "exit 0"
---

<objective>
Run the full test suite and CI checks after the complete upstream terminology rename (phases 1-7). Fix any failures.

Purpose: Verify that all code changes across phases 1-7 compile, pass tests, and meet CI quality gates.
Output: Green test suite and CI pipeline.
</objective>

<execution_context>
@/Users/shakefu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shakefu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and fix any failures</name>
  <files>(files depend on what fails — likely test files in tests/ or source files in src/)</files>
  <action>
Run `./script/test` and capture output. If any tests fail:
1. Read the failing test output to identify the root cause
2. Fix the failing code or test assertions — the cause will almost certainly be a missed rename from phases 1-7 (a "source repo" reference that should now say "upstream")
3. Re-run `./script/test` to confirm the fix
4. Repeat until all tests pass

Important: Do NOT weaken or remove tests. Fix the underlying issue (rename the missed reference) or update the assertion to match the new terminology. Test intent and coverage must remain identical.

If all tests pass on first run, proceed directly to Task 2.
  </action>
  <verify>
    <automated>cd /Users/shakefu/git/common-repo/common-repo && ./script/test</automated>
  </verify>
  <done>./script/test exits 0 with all tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Run CI checks and fix any issues</name>
  <files>(files depend on what fails — likely src/ files for fmt/clippy issues)</files>
  <action>
Run `./script/ci` and capture output. If any checks fail:
1. For cargo fmt failures: run `cargo fmt` to auto-fix
2. For clippy warnings: read the warning, fix the code issue (likely unused variables from renames, or naming convention warnings)
3. For pre-commit failures: fix the flagged issue
4. For prose check failures: fix any AI writing patterns in comments or docs
5. Re-run `./script/ci` to confirm all checks pass

If all checks pass on first run, this task is done.
  </action>
  <verify>
    <automated>cd /Users/shakefu/git/common-repo/common-repo && ./script/ci</automated>
  </verify>
  <done>./script/ci exits 0 with all checks green (fmt, clippy, pre-commit, prose)</done>
</task>

</tasks>

<verification>
Both ./script/test and ./script/ci exit 0 with no failures or warnings.
</verification>

<success_criteria>
- ./script/test passes with zero failures
- ./script/ci passes with all checks green
- No tests were removed or weakened to achieve green status
</success_criteria>

<output>
After completion, create `.planning/phases/08-final-validation/08-01-SUMMARY.md`
</output>
