---
phase: 08-final-validation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: ["(any files with remaining source repo references)"]
autonomous: true
requirements: [TEST-03, TEST-04]

must_haves:
  truths:
    - "No code identifiers, comments, or CLI output strings contain 'source repo' where it means the upstream repository"
    - "Merge operator source: field references are preserved unchanged"
    - "Final ./script/test and ./script/ci both pass after any audit fixes"
  artifacts:
    - path: "(no new files — this plan fixes existing files if needed)"
      provides: "Clean codebase with consistent upstream terminology"
  key_links:
    - from: "grep audit"
      to: "all .rs files in src/ and tests/"
      via: "grep -rn for source repo patterns"
      pattern: "0 matches for source.repo in code identifiers and comments"
---

<objective>
Perform a final grep audit for any remaining "source repo" references in the codebase (excluding the merge operator source: field). Fix any stragglers and confirm tests+CI still pass.

Purpose: Catch any references missed during phases 1-7 and ensure the rename is truly complete.
Output: Clean codebase with no stale "source repo" references; tests and CI confirmed green.
</objective>

<execution_context>
@/Users/shakefu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shakefu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-final-validation/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep audit for remaining "source repo" references</name>
  <files>(any .rs files in src/ or tests/ that still contain stale references)</files>
  <action>
Run a comprehensive grep audit across the codebase for remaining "source repo" terminology. Search for these patterns in .rs files:

1. `source.repo` (case-insensitive) in identifiers, comments, and strings — but EXCLUDE:
   - The merge operator `source:` field (in src/operators/ or similar) which refers to fragment file paths
   - Any .planning/ directory files
   - Any context/ directory files
2. `source_repo` in identifiers (variable names, struct fields, function names)
3. `"source repo"` or `"source repository"` in string literals and comments
4. `source-repo` in hyphenated references

For each match found:
- Determine if it refers to the upstream repository concept (rename to "upstream")
- Or if it refers to the merge operator source: field (leave unchanged)
- Make the fix

If any fixes are made, re-run `./script/test` and `./script/ci` to confirm nothing breaks.

If the audit finds zero remaining references, this task is done with no changes needed.
  </action>
  <verify>
    <automated>cd /Users/shakefu/git/common-repo/common-repo && grep -rn --include="*.rs" -i "source.repo\|source_repo" src/ tests/ | grep -v "//.*merge\|//.*operator\|// .*fragment\|source:" | head -20; echo "EXIT: $?"</automated>
  </verify>
  <done>Grep audit returns zero matches for "source repo" patterns in code (excluding merge operator source: field). If fixes were made, ./script/test and ./script/ci still pass.</done>
</task>

</tasks>

<verification>
1. Grep for "source repo" / "source_repo" patterns returns no matches in .rs files (excluding merge operator source: field)
2. ./script/test passes
3. ./script/ci passes
</verification>

<success_criteria>
- Zero remaining "source repo" references in code identifiers, comments, or CLI output (except merge operator source: field)
- All tests pass
- All CI checks pass
- The upstream terminology rename is complete
</success_criteria>

<output>
After completion, create `.planning/phases/08-final-validation/08-02-SUMMARY.md`
</output>
