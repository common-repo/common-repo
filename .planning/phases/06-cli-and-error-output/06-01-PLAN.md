---
phase: 06-cli-and-error-output
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/update.rs
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
must_haves:
  truths:
    - "Running `common-repo update --help` shows 'upstream' not 'source' in filter help text"
    - "Running `common-repo update --filter '...'` prints 'Filtering upstreams matching' not 'Filtering sources matching'"
    - "No error message in any src/*.rs file references 'source repo' where it means the upstream repository"
  artifacts:
    - path: "src/commands/update.rs"
      provides: "Updated CLI help text and output message"
      contains: "upstream"
  key_links:
    - from: "src/commands/update.rs"
      to: "CLI --help output"
      via: "clap doc comment on filter field"
      pattern: "Filter upstreams"
---

<objective>
Update all user-facing text in CLI help, output messages, and error messages to use "upstream" instead of "source repo".

Purpose: Complete the terminology rename for all text that end users see when running the CLI.
Output: Updated src/commands/update.rs with "upstream" terminology in help text and output.
</objective>

<execution_context>
@/Users/shakefu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shakefu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key decisions:
- Hard rename, no backwards compatibility shims
- Merge operator `source:` field must NOT be renamed (refers to fragment path)
- "source" -> "upstream" only where it refers to the source repository role

Scope note: After auditing the entire src/ directory, only two user-facing strings
reference "source" in the repository sense:
1. src/commands/update.rs line 75: clap doc comment "Filter sources by glob pattern"
2. src/commands/update.rs line 115: println!("Filtering sources matching: {}", patterns)

No error messages (src/error.rs, src/suggestions.rs) reference "source repo".
No other CLI help text references "source repo".
The "source" references in config.rs, merge/*.rs, and operators.rs refer to the
merge operator source: field (fragment file path), which is explicitly out of scope.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLI help text and output messages in update.rs</name>
  <files>src/commands/update.rs</files>
  <action>
In src/commands/update.rs, make these two changes:

1. Line 75: Change the clap doc comment from:
   `/// Filter sources by glob pattern (matches against url/path, scheme stripped).`
   to:
   `/// Filter upstreams by glob pattern (matches against url/path, scheme stripped).`

2. Line 115: Change the println from:
   `println!("Filtering sources matching: {}", patterns);`
   to:
   `println!("Filtering upstreams matching: {}", patterns);`

Do NOT change any other "source" references in this file or elsewhere -- the merge
operator `source:` field and code-level identifiers are handled in other phases.
  </action>
  <verify>
    <automated>cd /Users/shakefu/git/common-repo/common-repo && cargo build 2>&1 | tail -5</automated>
  </verify>
  <done>
  - The clap help text for --filter says "Filter upstreams" not "Filter sources"
  - The runtime output says "Filtering upstreams matching" not "Filtering sources matching"
  - Code compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify no remaining user-facing "source repo" references</name>
  <files></files>
  <action>
Run a comprehensive grep across all src/ files to confirm no remaining user-facing
strings (in println!, eprintln!, format!, clap doc comments, error messages, or
anyhow::anyhow! calls) reference "source repo" or "source repository" where it
means the upstream repository.

Specifically verify:
- src/error.rs: No error variant display text says "source repo"
- src/suggestions.rs: No suggestion text says "source repo"
- src/commands/*.rs: No help text or output messages say "source repo"
- src/cli.rs: No about/long_about says "source repo"
- src/main.rs: No output says "source repo"

The following are EXPECTED and should NOT be changed:
- Merge operator `source:` field references (src/config.rs, src/merge/*.rs)
- Code comments (handled in Phase 5)
- Code identifiers like `source_vars` (handled in earlier phases)
- Test files (handled in Phase 7)
  </action>
  <verify>
    <automated>cd /Users/shakefu/git/common-repo/common-repo && ! grep -rn 'source.repo\|source repository' src/commands/ src/cli.rs src/main.rs src/error.rs src/suggestions.rs src/output.rs 2>/dev/null | grep -v '^\s*//' | grep -v 'test' | grep -v 'source:' | head -20; echo "Exit: $?"</automated>
  </verify>
  <done>
  - Zero user-facing strings in commands/, cli.rs, main.rs, error.rs, suggestions.rs, or output.rs reference "source repo" in the upstream-repository sense
  - CLI-01 (help text), CLI-02 (output messages), and CLI-03 (error messages) requirements are satisfied
  </done>
</task>

</tasks>

<verification>
1. `cargo build` succeeds
2. `cargo test --lib` passes (no test breakage from string changes)
3. No user-facing "source repo" strings remain in CLI output, help text, or error messages
</verification>

<success_criteria>
- `common-repo update --help` shows "upstream" terminology in filter description
- Runtime output from `common-repo update --filter` says "Filtering upstreams matching"
- No error messages reference "source repo" where meaning is the upstream repository
- Code compiles and existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-cli-and-error-output/06-01-SUMMARY.md`
</output>
