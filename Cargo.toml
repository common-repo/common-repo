[package]
name = "common-repo"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
glob = "0.3"
regex = "1.10"
thiserror = "1.0"
anyhow = "1.0"
url = "2.5"
semver = "1.0"

[dev-dependencies]
tempfile = "3.0"
