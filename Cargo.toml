[package]
name = "common-repo"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
serde_json = "1.0"
toml = "0.8"
ini = "1.3"
pulldown-cmark = "0.10"
glob = "0.3"
regex = "1.10"
thiserror = "1.0"
anyhow = "1.0"
url = "2.5"
semver = "1.0"
walkdir = "2.5"

[dev-dependencies]
tempfile = "3.0"
datatest-stable = "0.1"

[features]
integration-tests = []

[[test]]
name = "schema_parsing_test"
harness = false
