{
  "task": "remove-duplicate-ci-build",
  "plan": null,
  "instructions": "Remove the build job from CI - it's slow and duplicates the build done in the test job. The test job already compiles the code as part of running tests, making the separate build job redundant."
}
