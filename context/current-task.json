{
  "task": "fix-automerge-workflow",
  "plan": null,
  "last_updated": "2026-01-02",
  "status": "pending",
  "description": "Fix auto-merge CI workflow that doesn't exclude itself when counting pending workflows",
  "context": {
    "bug": "Auto-merge workflow counts itself in pending workflows, causing infinite wait",
    "fix_needed": "Exclude the auto-merge workflow from the count of pending workflows",
    "file": ".github/workflows/auto-merge.yml"
  },
  "steps": [
    "Read .github/workflows/auto-merge.yml to understand current logic",
    "Identify where pending workflows are counted",
    "Add exclusion for the auto-merge workflow itself",
    "Test the fix if possible",
    "Commit and push"
  ]
}
