{
  "task": "fix-session-startup-test-command",
  "plan": null,
  "instructions": "Fix CLAUDE.md so the initial background test run does NOT use QUICK=1. This ensures dependencies (like prek, cargo-nextest) are installed and compiled on first run. Only subsequent test runs after making code changes should use QUICK=1."
}
