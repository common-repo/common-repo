# Additional workspace members to append
# This file contains members array that will be merged into workspace table
# When merged at path "workspace" with array_mode: append, these items are appended
members = ["new-crate-1", "new-crate-2"]
